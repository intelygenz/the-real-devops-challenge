stages:
  - test
  - build

test:
  stage: test
  image: docker:latest
  tags:
    - gitlab-org-docker
  script:
    - docker run -v $(pwd):/tmp/app -w /tmp/app --rm painless/tox /bin/bash tox

build:
  stage: build
  image: tmaier/docker-compose:latest
  script:
    - docker-compose build