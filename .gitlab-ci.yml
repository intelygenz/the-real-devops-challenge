
image: docker:latest

stages:
  - test
  - build

test:
  stage: test
  image: docker:latest
  services:
    - name: docker:19.03.8-dind

  script:
    - which docker
    - docker run -v $(pwd):/tmp/app -w /tmp/app painless/tox /bin/bash tox

# build:
#   stage: build
#   image: tmaier/docker-compose:latest
#   tags:
#     - gitlab-org-docker
#   script:
#     - docker-compose build