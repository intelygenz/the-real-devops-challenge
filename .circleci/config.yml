version: 2.1


jobs:
  test:  
    docker:
      - image: painless/tox 
    steps:
      - checkout
      - run:
          name: Run tests
          command: tox -p auto .

workflows:
  test-and-build: 
    jobs:
      - test
